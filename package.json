{
  "name": "app",
  "version": "1.0.0",
  "description": "Deploy To Vercel — AppForge",
  "scripts": {
    "build": "echo 'ready'"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}